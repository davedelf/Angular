import { MonitorDirective } from './monitor.directive';

describe('MonitorDirective', () => {
  it('should create an instance', () => {
    const directive = new MonitorDirective();
    expect(directive).toBeTruthy();
  });
});
