import { AutenticacionDirective } from './autenticacion.directive';

describe('AutenticacionDirective', () => {
  it('should create an instance', () => {
    const directive = new AutenticacionDirective();
    expect(directive).toBeTruthy();
  });
});
